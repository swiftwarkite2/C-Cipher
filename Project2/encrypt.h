#ifndef __ENCRYPT_H__

#define __ENCRYPT_H__
// defined variables to use in the other files
extern void encryptString(char* s, int n); // s is the message and n is the shift number
char ch;		// creates a char variable named ch
char code[20]; // creates an array of size 20 and type char named code
#endif
